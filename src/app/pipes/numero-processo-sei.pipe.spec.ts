import { NumeroProcessoSeiPipe } from './numero-processo-sei.pipe';

describe('NumeroProcessoSeiPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroProcessoSeiPipe();
    expect(pipe).toBeTruthy();
  });
});
